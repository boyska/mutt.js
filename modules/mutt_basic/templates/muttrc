# Reading

set mbox_type=Maildir

set folder="~/Mail/"
set mask="^."
set postponed="+drafts"
set record="+sent"

alternates "$alternates"

$filter_source_notmuch #source $conf_dir/mutt/notmuch

# ML

$maillists

source $conf_dir/mutt/sending

source $conf_dir/mutt/view
